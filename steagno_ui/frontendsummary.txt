=== app/page.tsx ===
"use client"

import { useRouter } from "next/navigation"
import { useEffect, useState } from "react"
import { ArrowRight, Lock, Volume2, Film } from "lucide-react"
import Link from "next/link"

export default function Home() {
  const router = useRouter()
  const [isAuthenticated, setIsAuthenticated] = useState(false)

  useEffect(() => {
    const token = localStorage.getItem("steagno_access_token")
    setIsAuthenticated(!!token)
  }, [])

  return (
    <main className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 text-white overflow-hidden">
      {/* Hero Section */}
      <section className="relative pt-32 pb-20 px-6">
        <div className="max-w-6xl mx-auto text-center">
          <h1 className="text-6xl md:text-7xl font-black mb-6 tracking-tight leading-tight">
            Multi-Media <span className="bg-gradient-to-r from-indigo-400 to-purple-400 bg-clip-text text-transparent">Steganography</span> System
          </h1>
          <p className="text-xl md:text-2xl text-slate-300 mb-8 max-w-3xl mx-auto">
            Hide secrets in plain sight. Embed confidential data into images, audio, and video with military-grade encryption.
          </p>

          {!isAuthenticated ? (
            <div className="flex gap-4 justify-center mb-16">
              <Link href="/login" className="px-8 py-4 bg-indigo-600 hover:bg-indigo-700 rounded-xl font-bold text-lg transition">
                Get Started <ArrowRight className="inline ml-2" size={20} />
              </Link>
              <Link href="/register" className="px-8 py-4 border border-purple-400 hover:bg-purple-400/10 rounded-xl font-bold text-lg transition">
                Sign Up
              </Link>
            </div>
          ) : (
            <div className="flex gap-4 justify-center mb-16">
              <Link href="/chat" className="px-8 py-4 bg-indigo-600 hover:bg-indigo-700 rounded-xl font-bold text-lg transition">
                Go to Editor <ArrowRight className="inline ml-2" size={20} />
              </Link>
            </div>
          )}
        </div>
      </section>

      {/* Features Section */}
      <section className="py-20 px-6 bg-black/20 backdrop-blur">
        <div className="max-w-6xl mx-auto grid md:grid-cols-3 gap-8">
          {/* Image Steganography */}
          <div className="p-8 rounded-2xl bg-gradient-to-br from-blue-500/10 to-blue-600/5 border border-blue-400/20">
            <Lock size={40} className="text-blue-400 mb-4" />
            <h3 className="text-2xl font-bold mb-2">Image Steganography</h3>
            <p className="text-slate-400">Embed secrets in PNG, JPEG, BMP using LSB & DCT algorithms. Invisible to the naked eye.</p>
          </div>

          {/* Audio Steganography */}
          <div className="p-8 rounded-2xl bg-gradient-to-br from-purple-500/10 to-purple-600/5 border border-purple-400/20">
            <Volume2 size={40} className="text-purple-400 mb-4" />
            <h3 className="text-2xl font-bold mb-2">Audio Steganography</h3>
            <p className="text-slate-400">Hide data in MP3, WAV files using echo hiding & LSB techniques. Zero quality loss.</p>
          </div>

          {/* Video Steganography */}
          <div className="p-8 rounded-2xl bg-gradient-to-br from-pink-500/10 to-pink-600/5 border border-pink-400/20">
            <Film size={40} className="text-pink-400 mb-4" />
            <h3 className="text-2xl font-bold mb-2">Video Steganography</h3>
            <p className="text-slate-400">Embed secrets across video frames. Frame-by-frame analysis with adaptive bitrate.</p>
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section className="py-20 px-6">
        <div className="max-w-4xl mx-auto text-center bg-gradient-to-r from-indigo-600/20 to-purple-600/20 border border-indigo-400/30 rounded-3xl p-12">
          <h2 className="text-4xl font-bold mb-4">Ready to Secure Your Data?</h2>
          <p className="text-lg text-slate-300 mb-8">Join thousands of users protecting their secrets with advanced steganography.</p>
          {!isAuthenticated && (
            <Link href="/register" className="inline-block px-8 py-4 bg-indigo-600 hover:bg-indigo-700 rounded-xl font-bold text-lg transition">
              Create Account Now
            </Link>
          )}
        </div>
      </section>
    </main>
  )
}

=== app/layout.tsx ===
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import "./globals.css"
import Navbar from "@/components/Navbar"

const geist = Geist({ subsets: ["latin"] })
const geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Steganography System",
  description: "Multi-media steganography for images, audio, and video",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={`${geist.variable} ${geistMono.variable} antialiased bg-slate-950 text-white`}>
        <Navbar />
        {children}
      </body>
    </html>
  )
}

=== app/login/page.tsx ===
"use client"

import { useState } from "react"
import { useRouter } from "next/navigation"
import Link from "next/link"
import { LogIn, AlertCircle } from "lucide-react"

export default function LoginPage() {
  const router = useRouter()
  const [email, setEmail] = useState("")
  const [password, setPassword] = useState("")
  const [error, setError] = useState("")
  const [isLoading, setIsLoading] = useState(false)

  const handleLogin = async (e: React.FormEvent) => {
    e.preventDefault()
    setError("")
    setIsLoading(true)

    try {
      const response = await fetch("http://localhost:8000/auth/login", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ email, password }),
      })

      const data = await response.json()

      if (!response.ok) {
        setError(data.detail || "Login failed")
        return
      }

      localStorage.setItem("steagno_access_token", data.access_token)
      localStorage.setItem("steagno_user", JSON.stringify(data.user))

      router.push("/chat")
      router.refresh()
    } catch (err) {
      setError("Connection error. Please try again.")
    } finally {
      setIsLoading(false)
    }
  }

  return (
    <main className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 flex items-center justify-center px-6 pt-20">
      <div className="w-full max-w-md">
        <div className="bg-slate-800/50 border border-slate-700/50 rounded-2xl p-8 backdrop-blur">
          <h1 className="text-3xl font-black mb-2 text-center">Sign In</h1>
          <p className="text-slate-400 text-center mb-8">Enter your credentials to access the steganography system</p>

          {error && (
            <div className="mb-6 p-4 bg-red-500/10 border border-red-500/50 rounded-lg flex items-center gap-2 text-red-400">
              <AlertCircle size={18} />
              {error}
            </div>
          )}

          <form onSubmit={handleLogin} className="space-y-4">
            <div>
              <label className="block text-sm font-bold mb-2">Email</label>
              <input
                type="email"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                placeholder="you@example.com"
                className="w-full px-4 py-3 bg-slate-700/50 border border-slate-600 rounded-lg focus:border-indigo-500 focus:outline-none"
                required
              />
            </div>

            <div>
              <label className="block text-sm font-bold mb-2">Password</label>
              <input
                type="password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                placeholder="••••••••"
                className="w-full px-4 py-3 bg-slate-700/50 border border-slate-600 rounded-lg focus:border-indigo-500 focus:outline-none"
                required
              />
            </div>

            <button
              type="submit"
              disabled={isLoading}
              className="w-full py-3 bg-indigo-600 hover:bg-indigo-700 disabled:opacity-50 rounded-lg font-bold flex items-center justify-center gap-2 transition"
            >
              <LogIn size={18} />
              {isLoading ? "Signing In..." : "Sign In"}
            </button>
          </form>

          <p className="text-center text-slate-400 mt-6">
            Don't have an account?{" "}
            <Link href="/register" className="text-indigo-400 hover:text-indigo-300 font-bold">
              Sign Up
            </Link>
          </p>
        </div>
      </div>
    </main>
  )
}

=== app/register/page.tsx ===
"use client"

import { useState } from "react"
import { useRouter } from "next/navigation"
import Link from "next/link"
import { UserPlus, AlertCircle } from "lucide-react"

export default function RegisterPage() {
  const router = useRouter()
  const [fullName, setFullName] = useState("")
  const [email, setEmail] = useState("")
  const [password, setPassword] = useState("")
  const [confirmPassword, setConfirmPassword] = useState("")
  const [error, setError] = useState("")
  const [isLoading, setIsLoading] = useState(false)

  const handleRegister = async (e: React.FormEvent) => {
    e.preventDefault()
    setError("")

    if (password !== confirmPassword) {
      setError("Passwords do not match")
      return
    }

    setIsLoading(true)

    try {
      const response = await fetch("http://localhost:8000/auth/register", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ full_name: fullName, email, password }),
      })

      const data = await response.json()

      if (!response.ok) {
        setError(data.detail || "Registration failed")
        return
      }

      router.push("/login")
    } catch (err) {
      setError("Connection error. Please try again.")
    } finally {
      setIsLoading(false)
    }
  }

  return (
    <main className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 flex items-center justify-center px-6 pt-20">
      <div className="w-full max-w-md">
        <div className="bg-slate-800/50 border border-slate-700/50 rounded-2xl p-8 backdrop-blur">
          <h1 className="text-3xl font-black mb-2 text-center">Create Account</h1>
          <p className="text-slate-400 text-center mb-8">Join the steganography community today</p>

          {error && (
            <div className="mb-6 p-4 bg-red-500/10 border border-red-500/50 rounded-lg flex items-center gap-2 text-red-400">
              <AlertCircle size={18} />
              {error}
            </div>
          )}

          <form onSubmit={handleRegister} className="space-y-4">
            <div>
              <label className="block text-sm font-bold mb-2">Full Name</label>
              <input
                type="text"
                value={fullName}
                onChange={(e) => setFullName(e.target.value)}
                placeholder="John Doe"
                className="w-full px-4 py-3 bg-slate-700/50 border border-slate-600 rounded-lg focus:border-indigo-500 focus:outline-none"
                required
              />
            </div>

            <div>
              <label className="block text-sm font-bold mb-2">Email</label>
              <input
                type="email"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                placeholder="you@example.com"
                className="w-full px-4 py-3 bg-slate-700/50 border border-slate-600 rounded-lg focus:border-indigo-500 focus:outline-none"
                required
              />
            </div>

            <div>
              <label className="block text-sm font-bold mb-2">Password</label>
              <input
                type="password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                placeholder="••••••••"
                className="w-full px-4 py-3 bg-slate-700/50 border border-slate-600 rounded-lg focus:border-indigo-500 focus:outline-none"
                required
              />
            </div>

            <div>
              <label className="block text-sm font-bold mb-2">Confirm Password</label>
              <input
                type="password"
                value={confirmPassword}
                onChange={(e) => setConfirmPassword(e.target.value)}
                placeholder="••••••••"
                className="w-full px-4 py-3 bg-slate-700/50 border border-slate-600 rounded-lg focus:border-indigo-500 focus:outline-none"
                required
              />
            </div>

            <button
              type="submit"
              disabled={isLoading}
              className="w-full py-3 bg-indigo-600 hover:bg-indigo-700 disabled:opacity-50 rounded-lg font-bold flex items-center justify-center gap-2 transition"
            >
              <UserPlus size={18} />
              {isLoading ? "Creating Account..." : "Sign Up"}
            </button>
          </form>

          <p className="text-center text-slate-400 mt-6">
            Already have an account?{" "}
            <Link href="/login" className="text-indigo-400 hover:text-indigo-300 font-bold">
              Sign In
            </Link>
          </p>
        </div>
      </div>
    </main>
  )
}

=== app/chat/page.tsx ===
"use client"

import { useEffect, useState } from "react"
import { useRouter } from "next/navigation"
import FileUpload from "@/components/FileUpload"
import { Upload, AlertCircle } from "lucide-react"

export default function ChatPage() {
  const router = useRouter()
  const [isAuthenticated, setIsAuthenticated] = useState(false)
  const [isLoading, setIsLoading] = useState(true)

  useEffect(() => {
    const token = localStorage.getItem("steagno_access_token")
    if (!token) {
      router.push("/login")
    } else {
      setIsAuthenticated(true)
      setIsLoading(false)
    }
  }, [router])

  if (isLoading) {
    return <div className="flex items-center justify-center min-h-screen">Loading...</div>
  }

  if (!isAuthenticated) {
    return null
  }

  return (
    <main className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 px-6 py-20 pt-32">
      <div className="max-w-4xl mx-auto">
        <h1 className="text-5xl font-black mb-4">Steganography Editor</h1>
        <p className="text-xl text-slate-400 mb-12">Upload your media file and choose an embedding algorithm</p>

        <div className="grid md:grid-cols-2 gap-8">
          {/* Upload Section */}
          <div className="bg-slate-800/50 border border-slate-700/50 rounded-2xl p-8 backdrop-blur">
            <h2 className="text-2xl font-bold mb-4 flex items-center gap-2">
              <Upload size={24} />
              Step 1: Upload Media
            </h2>
            <FileUpload />
          </div>

          {/* Algorithm Selection */}
          <div className="bg-slate-800/50 border border-slate-700/50 rounded-2xl p-8 backdrop-blur">
            <h2 className="text-2xl font-bold mb-4">Step 2: Select Algorithm</h2>
            <div className="space-y-3">
              {[
                { name: "LSB (Least Significant Bit)", desc: "Fast, basic embedding" },
                { name: "DCT (Discrete Cosine Transform)", desc: "JPEG-compatible" },
                { name: "DWT (Discrete Wavelet Transform)", desc: "High capacity" },
              ].map((algo) => (
                <button key={algo.name} className="w-full p-4 text-left bg-slate-700/50 hover:bg-slate-700 border border-slate-600 rounded-lg transition">
                  <div className="font-bold">{algo.name}</div>
                  <div className="text-sm text-slate-400">{algo.desc}</div>
                </button>
              ))}
            </div>
          </div>
        </div>
      </div>
    </main>
  )
}

=== app/extract/page.tsx ===
"use client"

import { useEffect, useState } from "react"
import { useRouter } from "next/navigation"
import { FileUp, AlertCircle } from "lucide-react"

export default function ExtractPage() {
  const router = useRouter()
  const [isAuthenticated, setIsAuthenticated] = useState(false)
  const [isLoading, setIsLoading] = useState(true)
  const [selectedFile, setSelectedFile] = useState<File | null>(null)
  const [extractedData, setExtractedData] = useState<string | null>(null)
  const [isExtracting, setIsExtracting] = useState(false)
  const [error, setError] = useState("")

  useEffect(() => {
    const token = localStorage.getItem("steagno_access_token")
    if (!token) {
      router.push("/login")
    } else {
      setIsAuthenticated(true)
      setIsLoading(false)
    }
  }, [router])

  const handleExtract = async (e: React.FormEvent) => {
    e.preventDefault()
    if (!selectedFile) {
      setError("Please select a file")
      return
    }

    setIsExtracting(true)
    setError("")

    const formData = new FormData()
    formData.append("file", selectedFile)

    try {
      const response = await fetch("http://localhost:8000/extract", {
        method: "POST",
        headers: {
          Authorization: `Bearer ${localStorage.getItem("steagno_access_token")}`,
        },
        body: formData,
      })

      const data = await response.json()

      if (!response.ok) {
        setError(data.detail || "Extraction failed")
        return
      }

      setExtractedData(data.extracted_data)
    } catch (err) {
      setError("Extraction error. Please try again.")
    } finally {
      setIsExtracting(false)
    }
  }

  if (isLoading) return <div className="flex items-center justify-center min-h-screen">Loading...</div>
  if (!isAuthenticated) return null

  return (
    <main className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 px-6 py-20 pt-32">
      <div className="max-w-2xl mx-auto">
        <h1 className="text-5xl font-black mb-4">Extract Hidden Data</h1>
        <p className="text-xl text-slate-400 mb-12">Upload a stego file to extract the hidden message</p>

        {error && (
          <div className="mb-6 p-4 bg-red-500/10 border border-red-500/50 rounded-lg flex items-center gap-2 text-red-400">
            <AlertCircle size={18} />
            {error}
          </div>
        )}

        <form onSubmit={handleExtract} className="bg-slate-800/50 border border-slate-700/50 rounded-2xl p-8 backdrop-blur">
          <div className="mb-6">
            <label className="block text-lg font-bold mb-4">Select Stego File</label>
            <input
              type="file"
              onChange={(e) => setSelectedFile(e.target.files?.[0] || null)}
              className="w-full px-4 py-3 bg-slate-700/50 border border-slate-600 rounded-lg"
              accept="image/*,audio/*,video/*"
            />
          </div>

          <button
            type="submit"
            disabled={isExtracting}
            className="w-full py-3 bg-indigo-600 hover:bg-indigo-700 disabled:opacity-50 rounded-lg font-bold flex items-center justify-center gap-2"
          >
            <FileUp size={18} />
            {isExtracting ? "Extracting..." : "Extract Data"}
          </button>

          {extractedData && (
            <div className="mt-8 p-6 bg-green-500/10 border border-green-500/50 rounded-lg">
              <h3 className="font-bold text-green-400 mb-2">Extracted Data:</h3>
              <p className="text-slate-300 break-words">{extractedData}</p>
            </div>
          )}
        </form>
      </div>
    </main>
  )
}

=== app/quality/page.tsx ===
"use client"

import { useEffect, useState } from "react"
import { useRouter } from "next/navigation"
import { BarChart3, AlertCircle } from "lucide-react"

export default function QualityPage() {
  const router = useRouter()
  const [isAuthenticated, setIsAuthenticated] = useState(false)
  const [isLoading, setIsLoading] = useState(true)
  const [metrics, setMetrics] = useState({
    psnr: 0,
    ssim: 0,
    capacity: 0,
    robustness: "high",
  })

  useEffect(() => {
    const token = localStorage.getItem("steagno_access_token")
    if (!token) {
      router.push("/login")
    } else {
      setIsAuthenticated(true)
      setIsLoading(false)
    }
  }, [router])

  if (isLoading) return <div className="flex items-center justify-center min-h-screen">Loading...</div>
  if (!isAuthenticated) return null

  return (
    <main className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 px-6 py-20 pt-32">
      <div className="max-w-4xl mx-auto">
        <h1 className="text-5xl font-black mb-4">Quality Metrics</h1>
        <p className="text-xl text-slate-400 mb-12">Analyze the quality of your stego files</p>

        <div className="grid md:grid-cols-2 gap-6">
          <div className="bg-slate-800/50 border border-slate-700/50 rounded-2xl p-8 backdrop-blur">
            <div className="flex items-center gap-2 mb-4">
              <BarChart3 size={24} className="text-indigo-400" />
              <h2 className="text-2xl font-bold">PSNR (dB)</h2>
            </div>
            <p className="text-4xl font-black text-indigo-400">{metrics.psnr.toFixed(2)}</p>
            <p className="text-slate-400 mt-2">Peak Signal-to-Noise Ratio</p>
          </div>

          <div className="bg-slate-800/50 border border-slate-700/50 rounded-2xl p-8 backdrop-blur">
            <h2 className="text-2xl font-bold mb-4">SSIM</h2>
            <p className="text-4xl font-black text-purple-400">{metrics.ssim.toFixed(3)}</p>
            <p className="text-slate-400 mt-2">Structural Similarity Index</p>
          </div>

          <div className="bg-slate-800/50 border border-slate-700/50 rounded-2xl p-8 backdrop-blur">
            <h2 className="text-2xl font-bold mb-4">Capacity</h2>
            <p className="text-4xl font-black text-green-400">{metrics.capacity} bits</p>
            <p className="text-slate-400 mt-2">Maximum data size</p>
          </div>

          <div className="bg-slate-800/50 border border-slate-700/50 rounded-2xl p-8 backdrop-blur">
            <h2 className="text-2xl font-bold mb-4">Robustness</h2>
            <p className="text-4xl font-black text-yellow-400">{metrics.robustness}</p>
            <p className="text-slate-400 mt-2">Attack resistance level</p>
          </div>
        </div>
      </div>
    </main>
  )
}